以下是整理好的问题总结，可以直接转给 DS。

---

# 《石窟天盾》参赛材料排查报告

**总体结论**：仓库的溯源纪律和数据管线基础是好的（时间划分无泄露、核心结果表几乎逐格可对上 results 落盘文件），但存在 **1 个已数值验证的核心实验 bug、1 个被自家结果否定的创新主张未披露、多处文档残留旧数字/口径失实**。答辩或评审前必须先处理高危项。

## 一、高危问题（必须修复）

**H1. exp03 核心实验的读出层特征顺序颠倒（已数值验证）**
- `code/src/operator/transport.py:123-130` 拟合算子时，快变特征按 **lag0=最新时刻在前**（时间降序）排列；
- 但 `code/experiments/exp03_risk_aligned.py:167-180` 的 `OperatorHead` 把 48h 窗口按 **时间升序** 展平后喂给读出层。
- 子代理用真实数据复算验证：按降序喂 `w_` 误差为 0，按代码当前的升序喂误差达 **7.1–12.7 个 RH 百分点**。即 exp03 的 A/B/C 三组全是“错位 lag 的读出”，**绝对指标（F1/AUC/R²）全部失真**，必须修正后重跑 exp03。

**H2. 核心创新主张被自家结果否定，但 README 仍作为头号贡献宣传**
- `code/results/exp03_verdict.csv`：9 组对比中 `C_wins` **全部 False**（F1_C − F1_B 为 −0.028 ~ −0.087）；
- `code/results/exp03_full_log.txt:86-88` 脚本自己打印“C 优于 B 的比例 0.0% => 判据未通过……不得在报告中夸大”；
- 但 `code/README.md:18,103-104` 仍把“算子导出的内生权重 / twCRPS 风险对齐”列为核心贡献，未披露该消融失败。文档表格里数字本身是诚实的（C 确实低于 B），问题在**叙事框架与自家判据矛盾**。这是答辩中最致命的一点。

**H3. 合成闭环自证，呈现顺序有夸大风险**
- 所有主指标（README 头条的 AUC 0.818 等）来自：`CaveModel.simulate()` 生成标签 → 算子在同一驱动上拟合同一标签 → 同分布评估。README 第 108-114 行虽有诚信声明，但**核心结果表与免责声明分离**，读者先看到 0.818。

**H4. derisk_01/02/03 在测试段切片构造特征，慢变特征系统性错误**
- `derisk_02_multihorizon.py:370-389`、`derisk_01_transport.py:282` 等对 `outdoor[te]` 切片后调用 `build_features`：1 年滑动均值在切片起点重新起步、48h 延迟坐标回填切片均值，与部署/训练时的特征分布不一致，**测试段指标被系统性污染**。
- 同仓库的 `exp03:274-275` 和 `build_demo_forecast.py:93-98` 做法正确（全序列先构造再切片），两种口径并存。

## 二、中危问题

| #    | 问题                                                         | 证据                                                      |
| ---- | ------------------------------------------------------------ | --------------------------------------------------------- |
| M1   | README 让用户跑 `calib_verify.py` 复现，但该脚本**不带自旋期**，会得到与 README 表格不同的数（无自旋 RH_in_max 虚高 14.67pp） | `calib_verify.py:21` vs `check_spinup_sensitivity.py:7-9` |
| M2   | 代码注释明确“气压必须用 PSC，不要用 PS”，但**所有实验特征全用 PS** | `src/data/power.py:69-84` vs `transport.py:56` 等四处     |
| M3   | exp03 的验证集算出来后**从未使用**（无早停/调参），beta=10 硬编码；且其时长误差口径正是 derisk_02 自己论证为“退化指标”的那种 | `exp03_risk_aligned.py:286-287, 234-238`                  |
| M4   | 各脚本 ridge beta 不一致（1e-6 / 1.0 / 10.0），README 头条表混合引用不同脚本的数字，不可横向比较 | `transport.py:76` 等                                      |
| M5   | FirstOrderTransfer 基线从单一初值对 4 万+小时递归推演不重置，发散是必然——README 把它当“初稿方案失效”的证据，属**稻草人对照** | `derisk_02:378-379`                                       |
| M6   | ICCP 消融显示 Magnus 项在验证段是**负贡献**（0.7301 > Full 0.7263），与“三族特征各有物理意义”的主张矛盾，脚本未判读 | `derisk02_full_log.txt:15-19`                             |

## 三、文档数字问题（多为过期残留，好修）

**对不上/失实的：**
1. `01_技术路线.md` §4.1：“预报最大值仅 **61.7%**” — 当前结果文件中不存在此数（实际 A 方案 pred_max=52.8），疑为旧标签轮残留。
2. `01_技术路线.md` §6.5 表：FirstOrderTransfer R² "**−∞（发散）**” — 实际 derisk02 测试段是 R²=−8.95（饱和非发散）；“发散”是 derisk01 的结果，**两个实验混进了同一张表**。
3. **“12 洞 → 8 有效洞”口径失实**：实际只有 9 个洞进入验证（9→8），3/8/10 号洞从未进入流程且无说明。01/03/06 三份文档都写错了。
4. 标定 “6 项目标中 **5 项达标**" 无出处：年极差比 0.4683 vs 0.559（−16%）、游客峰值 0.139 vs 11pp，至少 2 项明确未达标。
5. `06_佐证材料.md` 写 PPT "**19 页**"（实际 21 页）、`dist/README.txt` 写作品方案 "**11 页**"（实际 17 页）、README.txt 列出 **code.zip**（实际文件名是 `石窟天盾_参赛材料.zip`）。
6. `06_佐证材料.md` L103 换气次数"12 个洞窟 CO₂ 示踪法实测” — 与自家文献提取笔记矛盾：9–13/h 是 **45/46 窟**的示踪法结果，逐窟 ACH 是风速积分法，两种口径被混用。

**无落盘文件支撑的数字**（与 06 号文档“所有数值均可在 code/results 找到对应落盘文件”的承诺冲突）：
- Koopman rollout R² ≈ 0.003（三份文档写法还不一致：0.00/0.003/0.42/0.424）；
- 演示场景诊断 "34.8% / 52.0%"；
- 自旋期 "94.67%"；
- 旧标签消融 "C 优于 B 仅 2/9"（results 里只有新标签的 0/9）。

**核实通过的部分**（不用动）：exp03 A/B/C 数字、derisk02 基线/消融表、提前量 42.62h、读出层对比、跨窟 5/6、同窟同期对照、ICCP 8 洞统计、数据规模、PDF 页数大小——全部与 results 逐格吻合。

## 四、低危/卫生问题（简列)

- `validate_iccp.py:44-45` 硬编码相对路径（必须从仓库根运行）+ 全局屏蔽警告；
- `twcrps.py` 的积分参考实现**全仓库无调用**，训练目标与定义的一致性验证实际不存在；
- 敦煌 ISD 真实站数据（2015-2023）**没有任何代码消费**；
- NaN 回填用整块矩阵均值（切片时含未来行）；`rh_slow` clip 上限 120 来路不明；
- 演示起点按“未来 72h 真值最大 RH”挑选（代码注释自认，但前端未标注）；
- `pydmd`、`scoringrules` 等依赖零引用；部分死代码（`CaveModel.sensitivity` 等）。

## 五、修复优先级建议

1. **先修 H1**（OperatorHead 特征顺序），重跑 exp03，更新所有引用 exp03 数字的文档；
2. **README/03 号文档重写创新主张的表述**：要么如实写“内生权重消融未通过（0/9），C 方案不优于 B"，要么把卖点改为确实成立的部分（风险对齐的评估方法学、读出层改进 +15.2% 极值捕捉、ICCP 真实数据验证）；
3. 修正三、节列出的文档残留数字（半小时工作量，纯文案）；
4. H4 的切片特征问题如时间紧，至少在文档中披露该局限；M2（PS/PSC）建议改代码重跑或明确声明。